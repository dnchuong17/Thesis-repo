/**
 * Border radius tokens for consistent rounded corners across the app.
 * Usage: `style={{ borderRadius: theme.borderRadii.md }}`
 */
export const borderRadii = {
  sm: 4,
  md: 8,
  lg: 16,
  xl: 24,
  full: 9999,
} as const
